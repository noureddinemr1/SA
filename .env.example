BRIGHT_DATA_USERNAME=brd-customer-XXXXX-zone-XXXXX
BRIGHT_DATA_PASSWORD=your_password_here
CERTIFICATE_PATH=path/to/your/certificate.pfx
CERTIFICATE_PASSWORD=your_certificate_password

# Timing Configuration (Optional - only adjust if needed)
# These are optimized values based on hCaptcha validation requirements
# Default values work for most cases

# Wait time after captcha is solved (allows hCaptcha backend to validate)
# Default: 10 seconds (recommended: 8-12 seconds)
CAPTCHA_POST_SOLVE_WAIT=10

# Wait time before form submission (allows token to propagate)
# Default: 8 seconds (recommended: 6-10 seconds)
CAPTCHA_SUBMIT_DELAY=8

# Note: Total wait time = CAPTCHA_POST_SOLVE_WAIT + CAPTCHA_SUBMIT_DELAY
# Default total: 18 seconds (this prevents "Captcha inválido" errors)
# Reducing these values may cause captcha validation failures
# Only increase if you still get "Captcha inválido" errors
